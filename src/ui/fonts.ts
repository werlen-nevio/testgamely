// ─── FONTS ───
// Chosen deliberately (ART_DIRECTION.md §8), bundled locally via @fontsource so
// there is no runtime network dependency. The CSS faces are imported in main.ts.
//
// Cinzel — an inscriptional antiqua, for titles: reads like temple engravings.
// Space Mono — a clean geometric mono, for the HUD and all in-world numerals.

export const FONT_TITLE = '"Cinzel", Georgia, serif'
export const FONT_UI = '"Space Mono", ui-monospace, Menlo, monospace'
